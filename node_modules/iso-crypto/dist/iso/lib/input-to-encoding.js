export const inputToEncoding = (input)=>{
    if (typeof input === 'string') {
        return {
            text: input,
            encoding: 'utf8'
        };
    }
    if (input instanceof Uint8Array) {
        return {
            text: input,
            encoding: 'raw'
        };
    }
    return input;
};

//# sourceMappingURL=input-to-encoding.js.map